"""Candidate-centred integral job search.

One dimension model is the shared vocabulary across every stage — the questions
asked, the evidence extracted from an ad, the ranking, and later the application
and interview work. See `status/specification.md` for the argument and
`docs/METHODS.md` for every technique and formula the tool uses.
"""

from __future__ import annotations

__all__ = ["__version__"]

__version__ = "0.1.0"
