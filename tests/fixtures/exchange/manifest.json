{
  "manifest_version": 1,
  "comment": "The sources repository's published index, served offline from this directory by connector_exchange.directory_fetcher. `files` is the fetch list, not a description: install fetches exactly these names and nothing else, so a package file missing from this list is silently omitted from the installed copy and the fixture check then fails on it. Keep it in step with connectors/examplejobs_es/.",
  "connectors": [
    {
      "package": "examplejobs_es",
      "site": "examplejobs.test",
      "country": "ES",
      "language": "es",
      "contributor": "@nuncaeslupus",
      "last_verified": "2026-08-01",
      "files": [
        "connector.yaml",
        "meta.yaml",
        "fixture/list.html",
        "fixture/detail.html"
      ]
    }
  ]
}
