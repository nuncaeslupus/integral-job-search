## 2026-05-26

SKILL.md is not aligned with the guidelines:
  should line 3    description contains 5 quoted trigger strings — keyword-stuffing warn threshold  (R-XPOLL-10)
  should line ?    Heavy-handed "Never hardcode a model name" without explaining rationale inline  (R-BODY-(MUST-AVOID))
  should line ?    "non-negotiable precondition" is heavy-handed assertion styling  (R-BODY-(MUST-AVOID))
  should line ?    "Every fix or dismissal MUST be paired with a reply" — heavy-handed MUST styling in body prose  (R-BODY-(MUST-AVOID))
  should line 8    Body has zero `### ` sub-sections (only `## ` headings) — fails the ≥2 H3 subsections proxy  (R-XPOLL-6)

references/pr-review-loop.md is not aligned with the guidelines:
  should line ?    "Every fix or dismissal MUST be paired with a reply on the thread" — heavy-handed MUST styling  (R-BODY-(MUST-AVOID))
  should line ?    "Always include the agree/disagree/ambiguous rubric inline" — heavy-handed ALWAYS styling  (R-BODY-(MUST-AVOID))

(no MUST findings in this skill)
