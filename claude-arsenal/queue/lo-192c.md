# T43: Outside-the-advert enrichment, marked not from the ad

## Acceptance gate

```gate
outside_source_spans_in_explanations == 0
evidence: status/evidence/T43.json
key: outside_source_spans_in_explanations
```

```bash
echo "no gate command defined for T43 — replace this line with the command that writes status/evidence/T43.json" >&2; exit 1
```

## What this is

Plenty of adverts are four lines and a salary band. When extraction yields
very little, the step may look outside the advert — what the company does, how
it is spoken about, what former employees say — and add what it finds.

Everything found that way carries a provenance marker distinguishing it from the
advert, and the candidate sees which is which.

## The rule that matters

**Nothing sourced outside the advert ever appears as a verbatim evidence
span.** `explained_fraction` means the employer's own words, and an explanation
citing a review site as though the employer had written it is a lie about
provenance — the kind that is invisible until someone quotes it back in an
interview.

A candidate may switch outside lookups off as a standing preference, recorded in
`constraints.json`.

## Tests

Write these RED before any production code:

`test_outside_information_is_marked_not_from_the_advert` in
`tests/test_enrichment.py`;
`test_an_explanation_never_cites_an_outside_source_as_the_employer` — the gate;
`test_lookup_is_skipped_when_the_candidate_declined_it`.

## Location

Service: **MATCH** · Size: M · Depends: T15, T19

Design: `status/plan.md` (MATCH) · Spec: `status/spec-v2-steps.md` step 8 · `status/spec-v2-process.md` §2.6
