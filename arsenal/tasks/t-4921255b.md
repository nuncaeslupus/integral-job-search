---
id: t-4921255b
title: "T78: language_requirement as a hard field the ranker never reads"
priority: 5
deps: [t-e6f1dc24]
workspace: MATCH
tags: [v3, m5]
---

# T78: language_requirement as a hard field the ranker never reads

## Acceptance gate

```gate
gate_fields_read_by_the_ranker == 0
evidence: status/evidence/T78.json
key: gate_fields_read_by_the_ranker
status-key: gate_status
```

```bash
uv run --extra dev pytest tests/test_eligibility.py tests/test_ranking.py -q
uv run --extra dev python -m integral.eligibility
```

`src/integral/eligibility.py` must write `status/evidence/T78.json`. This is a new module, so it owns this record outright. **A gate must never name a file no module produces** — the file not existing yet is the honest state of an unstarted task; the file existing but empty of this metric is not.

The `bash` block regenerates it; the `gate` block asserts the number in it.

## Why

**Settled by the owner on 2026-08-25:** the language gate gets its own hard field rather than reading `dimensions/english_demand.yaml`.

The dimension is a soft, scored preference the ranker weights. The gate is a hard filter. Merging them would let a preference weight cancel a legal or linguistic bar — a failure that would be **invisible in any output either layer produces**, which is exactly why the boundary is pinned by tests rather than by a comment.

`offer.language_requirement` records `{language, level_stated, quote, applies_to}` and is read only by the gate. See `status/spec-v3-silent-success.md` §5.1 and §5.3.

Adapted from `MadsLorentzen/ai-job-search` (MIT, © 2026 Mads Lorentzen). **T83 records the attribution** — do not add a credit line here; it belongs in `README.md` and `docs/METHODS.md`, in one place, once.

## What it must not become

The distinction that is easy to get wrong: read the language requirement **stated for the role**, not the language the advert happens to be written in. A Catalan advert for a role needing only Spanish demands Spanish.

The boundary test must fail in **both** directions — ranker reading a gate field, and gate reading a weight.

**A zero-violation count over an empty input set is not a pass.** The gate counts
violations, and nothing counted is also zero — so the evidence record must carry
`gate_fields_read_by_the_ranker_evaluated` (gate/ranker field reads audited) and the gate is only meaningful while that count is
non-zero. This is the failure this whole increment is about, turned on its own gates:
a check that reports success over work it did not do. Assert the denominator.

## Tests — write these RED first

`test_a_hard_gate_field_is_never_read_by_the_ranker` in `tests/test_eligibility.py`.

`test_a_dimension_weight_cannot_change_a_gate_verdict` — vary the weight, assert the verdict is identical.

`test_the_role_language_is_read_not_the_adverts_own_language` — a Spanish-language advert requiring English is about English.

`test_the_gate_does_not_pass_on_an_empty_input_set` — assert `gate_fields_read_by_the_ranker_evaluated` is written and non-zero.

## Location

Service: **MATCH** · Size: M

Spec: `status/spec-v3-silent-success.md` · Plan: `status/plan.md` (T78) ·
Methods: `docs/METHODS.md`

## A zero count must prove the mechanism ran — 2026-08-26

This gate asserts a **violation count of zero**, and an empty input set produces
zero too. As written it could pass without evaluating a single offer, verdict,
ranked result, document, keyword, application or technique — which is *precisely*
the failure class this increment exists to catch, reproduced inside its own
acceptance criteria.

The gate block therefore carries `status-key: gate_status`, and the producing
module must honour it:

* record **``ranked_offers_evaluated``** — how many inputs were actually evaluated — in the evidence
  file, beside the violation count;
* write **`gate_status: "unmeasured"`** whenever that count is `0`, and
  `"measured"` otherwise.

`gate_evidence.py` reads `status-key` before it reads the metric and exits **3** on
`unmeasured` — "the check ran, and what it found is that this cannot be scored
yet". Not a pass and not a fail, which is the honest third outcome for a run that
processed nothing. That is the same mechanism `lo-6f53` uses for
`extraction_macro_f1`, so this is existing machinery rather than a new rule.

**A second assertion in the gate block would not have worked**: line 1 of a `gate`
fence *is* the gate, one metric per block. Making the emptiness visible through the
status key is what makes the invariant executable rather than prose.
