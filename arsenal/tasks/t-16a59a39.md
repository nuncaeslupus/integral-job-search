---
id: t-16a59a39
title: "T64: Symmetric scope proposals: a one-way proposal cannot be constructed"
priority: 5
deps: [t-bbb3d1da]
workspace: SUPPLY
---

## Acceptance gate

```gate
scope_proposals_offering_only_narrowing == 0
evidence: status/evidence/T64.json
key: scope_proposals_offering_only_narrowing
```

```bash
# e.g. bash tests/surface_probe_test.sh
false  # fail until a real check replaces this
```

The `gate` block is settled by the design and is read from the default branch —
it is the assertion, and is not this task's to weaken. The `bash` block is the
shipped placeholder and **is** this task's to replace: it must regenerate
`status/evidence/T64.json`. A task whose measurement is undefined has not passed
its gate; it has not been measured.

## Tests

Write these RED before any production code:

`test_a_proposal_offering_only_narrowing_is_rejected` in `tests/test_sourcing_strategy.py` — construction fails without an opposite-direction alternative

`test_a_widening_is_proposable_when_the_search_is_too_focused`

`test_every_proposal_states_its_reason_in_candidate_terms`

## Design

`status/specs/iterative-sourcing.md` §5.4 · `status/plan.md` row T64.

Part of **iterative sourcing** — sourcing sits outside the 6 → 9 → 10 loop, so the
offer set is fixed by the least-informed moment in the process. The first blind
sitting measured the cost: of twenty adverts, one was worth applying to.
