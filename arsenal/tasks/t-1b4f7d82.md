---
id: t-1b4f7d82
title: "T189: Build a connector for remoteai.io (deferred \u2014 do not start without a go-ahead)"
priority: 1
tags: [connectors]
---

Candidate note (Ivan, 2026-09-16): he found a promising posting via
https://remoteai.io. This board has no connector yet and is not listed in
`connectors/ruled-out.yaml` or `robots-adjudications.yaml` — genuinely uncatalogued.

Per `step-07-sourcing`, building a connector must be proposed to the owner first
(token cost), and the owner has said not to build it yet — this task exists only to
record the candidate board and its rationale for when/if it's approved. Do not start
implementation without an explicit go-ahead, and when it is approved, follow
`connector-new`'s step 1 (robots/ToS check) first, since that alone can rule the
board out before any scraping code is written.


## Acceptance gate

<!-- Replace this with a fenced bash block. A gate that is only prose runs
     nothing, and a gate that runs nothing passes everything — `task_select.py`
     reports gate: false for a task with no block, so an unenforced gate is
     visible rather than quietly inert. -->

```bash
# arsenal:gate-placeholder — replace with the real check; it may land in this task's own PR
# e.g. bash tests/surface_probe_test.sh
false
```
