---
id: t-506d8fa5
title: "T94: Hundreds of adverts arrive and nothing filters them but a person"
priority: 5
tags: [SOURCING]
status: merged
---

> Regarding evaluation of hundreds of ads, don't you have some automatic
> tooling? If not, you should. So retrieving a lot of jobs with connectors
> (scrapers) and filtering them automatically should be offered by the tool.

The connector library can now return volume — thirteen packages, and the
JSON-reading ones return whole result sets in one request. The pipeline behind
them was built for the handful of adverts a manual round produced, so volume
arrives and is triaged by reading.

This is what makes T91 safe to build: a loop that keeps adding connectors is
only an improvement if what they return can be reduced without a person in the
loop. Otherwise more coverage is more work.

Bounded deliberately: **filter, not rank**. Ranking is step 9 and has an owner.
This is the cheap pass before it — hard constraints, eligibility, expiry and
duplicates — reducing a few hundred rows to the tens that are worth
understanding.

**Stated exclusions are not on that list, and must not be.** T90 defines a sector
distaste as *soft*: it shapes the query, and where a strong match survives it the
survival is named. If this filter also deleted rows on an exclusion, the two
tasks would disagree about the same rule and the stricter one would win silently
— the candidate would lose a strong match with no reason given, which is the
opposite of what T90 asks for. Exclusions reach the **query**; they never delete
a fetched row here.

## Acceptance gate

```gate
bulk_offers_requiring_manual_triage == 0
evidence: status/evidence/T94.json
key: bulk_offers_requiring_manual_triage
```

The fenced `gate` block was **added when this task was implemented**, not when it
was filed: `verify_gates.py` reads only this fence, so on the archive that makes a
task terminal the row reported `1 carry no fenced gate block`. The prose and the
`bash` recipe below are unchanged.

The metric is **`status/plan.md`'s own** — `bulk_offers_requiring_manual_triage`,
declared in T94's plan row before this implementation existed, which is what keeps
the number from being one the implementer chose to suit the code. A row requiring
manual triage is one this pass did not settle: it reached neither bucket, or it was
dropped naming no rule or no reason. It is deliberately *not* `reduction_ratio` —
a ratio gate rewards deleting more, which is the wrong direction for a filter whose
costly error is the drop.

A zero is also what an empty scan reports, so the number alone is not the check.
`integral.bulk_filter._main` **returns 1** — never 3, which `Makefile:58-70`
records and continues past — on four further breaches that a do-nothing filter
would trip: `offers_in` under its floor, any `soft_preference_drops`, any rule the
probe batch never exercised, and the planted crosspost failing to collapse.

```bash
uv run --extra dev pytest tests/test_bulk_filter.py -q
uv run --extra dev python -m integral.sourcing_cycles --bulk
```

- `test_a_few_hundred_offers_reduce_without_a_human_decision`
- `test_every_rejection_records_which_rule_dropped_it` — a filter that cannot say
  why is a filter nobody can trust or debug.
- `test_the_filter_never_drops_on_a_soft_preference` — soft goes to ranking, and
  only hard rules delete.
- `test_a_stated_exclusion_never_deletes_a_fetched_row` — the T90 boundary,
  asserted from this side too.
- `test_a_strong_match_surviving_an_exclusion_arrives_with_its_reason`
