---
id: lo-e497
title: "T4b: [LAPTOP] Collect \u2265100 raw ads (\u224860 ES, 25 EN, 15 CA) for remote programming roles \u2014 text + source URL only, no labels"
priority: 85
workspace: ONTOLOGY
tags: [laptop]
status: merged
pr: https://github.com/nuncaeslupus/job-search/pull/6
---

## Acceptance gate

```gate
raw_ad_count >= 100
evidence: status/evidence/T4b.json
key: raw_ad_count
```

```bash
uv run python -m jobsearch.corpus status/evidence/T4b.json
uv run --extra dev pytest tests/test_corpus_raw.py -q
```

The gate measures the *committed* corpus and needs no egress: collection is the
laptop-only step (`tools/collect_ads.py`, `--extra collect`), verification is not.

The two blocks do different jobs and both are required. The `bash` block
regenerates `status/evidence/T4b.json`; the `gate` block asserts the
number in it against the threshold. Without the first, a stale or hand-written
evidence file passes unchallenged; without the second, a command that exits 0
counts as a gate whatever it measured.

The default command fails on purpose. A task whose measurement is undefined has
not passed its gate — it has not been measured. Replace it as part of the work.

## Tests

Write these RED before any production code:

`test_raw_corpus_meets_size_and_language_mix` in `tests/test_corpus_raw.py` — ≥100 raw ads and language mix within ±10% of target; `test_every_raw_ad_carries_a_source_url` — an entry without a resolvable source URL is refused at load

## Location

Service: **ONTOLOGY** · Size: M

Design: `status/plan.md` (T4b) · Spec: `status/specification.md` §5 contracts · Methods: `docs/METHODS.md`

## Laptop-only

Needs egress to job boards, which the cloud session's policy denies (403 at
the proxy). Tagged `laptop`; release.sh refuses `done` from a cloud session
via CLAUDE_CODE_REMOTE, so a cloud worker cannot falsely complete it.
