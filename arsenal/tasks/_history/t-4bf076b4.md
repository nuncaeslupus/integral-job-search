---
id: t-4bf076b4
title: "T128: a connector cannot declare its charset, so any board not serving UTF-8 is unreadable"
priority: 5
status: merged
---

Imported from issue #355.

## Acceptance gate

```gate
charset_contracts_failing == 0
evidence: status/evidence/T128.json
key: charset_contracts_failing
```

```bash
uv run --extra dev pytest tests/test_charset.py -q
uv run python -m integral.connectors
```
