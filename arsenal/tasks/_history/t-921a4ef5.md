---
id: t-921a4ef5
title: "T74: Liveness checks page identity, not only the URL"
priority: 5
deps: []
workspace: SUPPLY
tags: [v3, m5]
status: merged
---

# T74: Liveness checks page identity, not only the URL

## Acceptance gate

```gate
offers_presented_from_a_page_that_is_not_the_advert == 0
evidence: status/evidence/T74.json
key: offers_presented_from_a_page_that_is_not_the_advert
status-key: gate_status
```

```bash
uv run --extra dev pytest tests/test_liveness.py -q
uv run --extra dev python -m integral.liveness
```

`src/integral/liveness.py` must write `status/evidence/T74.json`. It already writes `status/evidence/D-18.json` for another task — add this record beside it rather than replacing it, so both gates keep reading. **A gate must never name a file no module produces** — the file not existing yet is the honest state of an unstarted task; the file existing but empty of this metric is not.

The `bash` block regenerates it; the `gate` block asserts the number in it.

## Why

`liveness.read_response` compares `advert_url` against `final_url` via `same_page` (`liveness.py:120,148`). That catches the **redirect** case and misses the **identity** case: a stored URL like `.../jobs/ciso/#ikerian` fetches 200, renders a listing page of unrelated roles, and the URL never changed — so it reads as the advert and gets scored.

This is the D-18 family: schema validity is not liveness, and now — a live page is not *this* page. Add a fourth reason for `unverified`, not a fourth verdict.

Adapted from `MadsLorentzen/ai-job-search` (MIT, © 2026 Mads Lorentzen). **T83 records the attribution** — do not add a credit line here; it belongs in `README.md` and `docs/METHODS.md`, in one place, once.

## What it must not become

**Do not add a new verdict.** The three-valued vocabulary is settled; this is one more way to reach `unverified`.

Match on the offer's title against the fetched body, tolerantly — casefold, collapse whitespace. A title match that is too strict turns live adverts into `unverified` and the candidate sees nothing, which is a worse failure than the one being fixed.

**A zero-violation count over an empty input set is not a pass.** The gate counts
violations, and nothing counted is also zero — so the evidence record must carry
`offers_presented_from_a_page_that_is_not_the_advert_evaluated` (offers liveness-checked) and the gate is only meaningful while that count is
non-zero. This is the failure this whole increment is about, turned on its own gates:
a check that reports success over work it did not do. Assert the denominator.

## Tests — write these RED first

`test_a_fragment_anchor_landing_on_a_listing_page_is_unverified` in `tests/test_liveness.py` — the URL is unchanged, so `same_page` passes and the check must still refuse.

`test_a_page_whose_title_does_not_match_the_offer_is_unverified`.

`test_the_advert_itself_still_reads_live` — the fix must not make everything `unverified`.

`test_the_gate_does_not_pass_on_an_empty_input_set` — assert `offers_presented_from_a_page_that_is_not_the_advert_evaluated` is written and non-zero.

## Location

Service: **SUPPLY** · Size: M

Spec: `status/spec-v3-silent-success.md` · Plan: `status/plan.md` (T74) ·
Methods: `docs/METHODS.md`

## A zero count must prove the mechanism ran — 2026-08-26

This gate asserts a **violation count of zero**, and an empty input set produces
zero too. As written it could pass without evaluating a single offer, verdict,
ranked result, document, keyword, application or technique — which is *precisely*
the failure class this increment exists to catch, reproduced inside its own
acceptance criteria.

The gate block therefore carries `status-key: gate_status`, and the producing
module must honour it:

* record **``pages_checked``** — how many inputs were actually evaluated — in the evidence
  file, beside the violation count;
* write **`gate_status: "unmeasured"`** whenever that count is `0`, and
  `"measured"` otherwise.

`gate_evidence.py` reads `status-key` before it reads the metric and exits **3** on
`unmeasured` — "the check ran, and what it found is that this cannot be scored
yet". Not a pass and not a fail, which is the honest third outcome for a run that
processed nothing. That is the same mechanism `lo-6f53` uses for
`extraction_macro_f1`, so this is existing machinery rather than a new rule.

**A second assertion in the gate block would not have worked**: line 1 of a `gate`
fence *is* the gate, one metric per block. Making the emptiness visible through the
status key is what makes the invariant executable rather than prose.
