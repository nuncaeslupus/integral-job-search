---
id: lo-0204
title: "T1: Scaffold package: uv, ruff, strict mypy, pytest, Makefile, `profiles/` gitignored"
priority: 100
workspace: ONTOLOGY
status: merged
pr: https://github.com/nuncaeslupus/job-search/pull/4
---

## Acceptance gate

```gate
lint_typecheck_exit_code == 0
evidence: status/evidence/T1.json
key: lint_typecheck_exit_code
```

```bash
make gate
```

The two blocks do different jobs and both are required. The `bash` block
regenerates `status/evidence/T1.json`; the `gate` block asserts the
number in it against the threshold. Without the first, a stale or hand-written
evidence file passes unchallenged; without the second, a command that exits 0
counts as a gate whatever it measured.

The default command fails on purpose. A task whose measurement is undefined has
not passed its gate — it has not been measured. Replace it as part of the work.

## Tests

Write these RED before any production code:

`test_package_imports_cleanly_exposes_version` in `tests/test_scaffold.py` — importing `jobsearch` yields a semver `__version__`

## Location

Service: **ONTOLOGY** · Size: S

Design: `status/plan.md` (T1) · Spec: `status/specification.md` §5 contracts · Methods: `docs/METHODS.md`
